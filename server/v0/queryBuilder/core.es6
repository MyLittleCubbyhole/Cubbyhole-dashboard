/*Parent class cloning*/

	var Module = require('kanto-patterns').module.clone(__dirname);

/*Attributes definitions*/

	Module._name = 'QUERY_BUILDER';
	Module._routing = false;

module.exports = Module;